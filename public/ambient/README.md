Add ambient audio files here (optional).

Place two files if you want recorded ambient sounds to be used instead of procedural noise:
- rain.mp3
- sea.mp3

File names must match exactly: `/ambient/rain.mp3` and `/ambient/sea.mp3`.

If not present, the app falls back to generated ambient noise.