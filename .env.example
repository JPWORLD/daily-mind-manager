# Copy to .env and fill values for production
# Example Postgres URL:
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public
DATABASE_URL=

# Admin token for API write operations (set a strong secret)
ADMIN_TOKEN=replace-with-a-secure-token

# (Optional) Firebase service account JSON string for verification
# FIREBASE_SERVICE_ACCOUNT={...}

# WAQI token (replace demo)
WAQI_TOKEN=

# Model provider (optional) — set to the model you want clients to use
# Example: MODEL_NAME=claude-haiku-4.5
MODEL_NAME=claude-haiku-4.5

# Backwards-compatible env name used by E2E tests
# `DMM_ADMIN_TOKEN` will be used if `ADMIN_TOKEN` is not set.
DMM_ADMIN_TOKEN=replace-with-a-secure-token
